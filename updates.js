/*
function getCookie(cname) {
  let name = cname + "=";
  let decodedCookie = decodeURIComponent(document.cookie);
  let ca = decodedCookie.split(';');
  for(let i = 0; i <ca.length; i++) {
    let c = ca[i];
    while (c.charAt(0) == ' ') {
      c = c.substring(1);
    }
    if (c.indexOf(name) == 0) {
      return c.substring(name.length, c.length);
    }
  }
  return "";
}
pushNotification = getCookie("updatesViewed")
if (pushNotification === null) {
	alert("Version 1.0 | goosegame\r\n 1. Changes: \r\n 1. goose game is out of beta \r\n 2. Kicked a few players from the game because they were bugging playerCollision and updated anticheat and afk systems \r\n 3. Recruited 3 new testers (they chose not to have their names disclosed) ")
	document.cookie = "updatesViewed = true";
}


*/
//so this is a updates page but i got a simpler version in index.html
//go see what happens when you go to homepage of goosegame
//you want a different design?
//I think we should put the updates on a h1 in the main menu :|
//k that works
//Yoo larry wait i need to t

//saving this file for archive purposes